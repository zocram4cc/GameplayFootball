name: CI

on:
  push:
    branches: [ "main", "master", "copilot/**", "claude/**", "cursor/**" ]
  pull_request:
    branches: [ "main", "master" ]

permissions:
  contents: read

jobs:
  format-check:
    name: Check formatting (clang-format)
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install clang-format
        # Pinned via pip so contributors and CI agree on the version; the apt
        # clang-format (14) formats several constructs differently from modern
        # releases, which made the check fail on correctly formatted code.
        run: pipx install clang-format==22.1.8

      - name: Check changed files
        run: |
          BASE="${{ github.event.pull_request.base.sha || 'HEAD~1' }}"
          FILES=$(git diff --name-only --diff-filter=d "$BASE"...HEAD -- '*.cpp' '*.hpp' 2>/dev/null || true)
          if [ -z "$FILES" ]; then
            echo "No C++ files changed – skipping format check."
            exit 0
          fi
          echo "Checking format of: $FILES"
          echo "$FILES" | xargs clang-format --dry-run --Werror

  # Task 2.2: headless test job – builds and runs all tests with no SDL/OpenGL
  # dependency, proving tests run without a display server.
  test-headless:
    name: Headless tests (no display required)
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install minimal dependencies
        run: |
          bash scripts/setup_linux_deps.sh --minimal
          # gamedefines.hpp pulls SDL for key ids (headless job 97387041457, run 32712604651)
          sudo apt-get install -y --no-install-recommends libsdl2-dev 2>/dev/null || true

      - name: Configure (tests only, no game build)
        run: |
          cmake -S . -B build-headless \
            -DCMAKE_BUILD_TYPE=Debug \
            -DGAMEPLAYFOOTBALL_BUILD_GAME=OFF \
            -DBUILD_TESTING=ON

      - name: Build tests
        run: cmake --build build-headless --parallel 2

      - name: Run tests (headless – no DISPLAY needed)
        run: ctest --test-dir build-headless --output-on-failure

  clang-tidy:
    name: Static analysis (clang-tidy)
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: bash scripts/setup_linux_deps.sh --with-tools

      - name: Configure (generate compile_commands.json)
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Run clang-tidy on changed files
        run: |
          BASE="${{ github.event.pull_request.base.sha || 'HEAD~1' }}"
          FILES=$(git diff --name-only --diff-filter=d "$BASE"...HEAD -- 'src/*.cpp' 'src/**/*.cpp' 2>/dev/null || true)
          if [ -z "$FILES" ]; then
            echo "No C++ source files changed – skipping clang-tidy."
            exit 0
          fi
          echo "Running clang-tidy on: $FILES"
          echo "$FILES" | xargs -r clang-tidy -p build \
            --header-filter='src/.*' 2>&1 | tee tidy-output.txt
          grep -c "error:" tidy-output.txt && exit 1 || true

  build-linux:
    name: Build (Ubuntu 22.04)
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install dependencies
        run: bash scripts/setup_linux_deps.sh

      - name: Configure (Debug)
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Build (Debug)
        run: cmake --build build --parallel 3

      - name: Test (Debug)
        run: ctest --test-dir build --output-on-failure

      # Smoke tests were trimmed from CI to save runner minutes; run them
      # locally with tests/run_menu_smoke.sh and the configs under data/.

      - name: Configure (Release)
        run: |
          cmake -S . -B build-release \
            -DCMAKE_BUILD_TYPE=Release

      - name: Build (Release)
        run: cmake --build build-release --parallel 3

      - name: Stage Linux release package
        run: |
          mkdir -p dist/linux-x64
          cp build-release/gameplayfootball dist/linux-x64/
          cp build-release/football.config dist/linux-x64/
          cp -R build-release/media build-release/databases build-release/locale dist/linux-x64/
          mkdir -p dist/linux-x64/data
          cp build-release/data/football.config dist/linux-x64/data/
          cp -R build-release/data/locale dist/linux-x64/data/

      - name: Upload Linux release artifact
        uses: actions/upload-artifact@v7
        with:
          name: gameplayfootball-linux-x64
          path: dist/linux-x64/
          if-no-files-found: error

  build-macos:
    name: Build (macOS 14)
    runs-on: macos-14

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install dependencies
        env:
          HOMEBREW_NO_AUTO_UPDATE: 1
        run: |
          brew update
          HOMEBREW_NO_AUTO_UPDATE=1 brew install \
            cmake \
            sdl2 sdl2_image sdl2_ttf sdl2_gfx \
            boost openal-soft sqlite

      - name: Configure (Debug)
        run: |
          cmake -S . -B build-macos \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Build (Debug)
        run: cmake --build build-macos --parallel 3

      - name: Test (Debug)
        run: ctest --test-dir build-macos --output-on-failure

      - name: Configure (Release)
        run: |
          cmake -S . -B build-macos-release \
            -DCMAKE_BUILD_TYPE=Release

      - name: Build (Release)
        run: cmake --build build-macos-release --parallel 3

  build-windows:
    name: Build (Windows 2022 + MSVC + vcpkg)
    runs-on: windows-2022
    env:
      VCPKG_ROOT: ${{ github.workspace }}\vcpkg
      VCPKG_DEFAULT_TRIPLET: x64-windows

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Bootstrap vcpkg
        shell: pwsh
        run: |
          git clone https://github.com/microsoft/vcpkg $env:VCPKG_ROOT
          & "$env:VCPKG_ROOT\bootstrap-vcpkg.bat" -disableMetrics

      - name: Cache vcpkg packages
        uses: actions/cache@v6
        with:
          path: |
            ${{ env.VCPKG_ROOT }}/downloads
            ${{ github.workspace }}/build-win/vcpkg_installed
          key: vcpkg-win-${{ hashFiles('vcpkg.json') }}-${{ env.VCPKG_DEFAULT_TRIPLET }}
          restore-keys: |
            vcpkg-win-${{ hashFiles('vcpkg.json') }}-
            vcpkg-win-

      - name: Configure
        shell: pwsh
        run: |
          cmake -S . -B build-win `
            -G "Visual Studio 17 2022" -A x64 `
            -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_ROOT\scripts\buildsystems\vcpkg.cmake" `
            -DVCPKG_TARGET_TRIPLET="$env:VCPKG_DEFAULT_TRIPLET"

      - name: Build (Debug)
        shell: pwsh
        run: cmake --build build-win --config Debug --parallel 4

      - name: Test (Debug)
        shell: pwsh
        run: ctest --test-dir build-win -C Debug --output-on-failure

      - name: Build (Release)
        shell: pwsh
        run: cmake --build build-win --config Release --parallel 4

      - name: Package runnable Windows build
        shell: pwsh
        run: |
          .\scripts\package_windows.ps1 `
            -BuildDir "build-win\Release" `
            -OutDir "dist\windows-x64" `
            -VcpkgRoot $env:VCPKG_ROOT `
            -Triplet $env:VCPKG_DEFAULT_TRIPLET `
            -RepoRoot $env:GITHUB_WORKSPACE

      - name: Upload Windows release artifact
        uses: actions/upload-artifact@v7
        with:
          name: gameplayfootball-windows-x64
          path: dist/windows-x64/
          if-no-files-found: error

  # Task 5.1: build Doxygen HTML docs and upload as a CI artifact
  docs:
    name: Generate Doxygen docs
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Doxygen
        run: sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends doxygen graphviz cmake build-essential libboost-dev

      - name: Generate Doxyfile from template
        run: |
          cmake -S . -B /tmp/build-docs \
            -DGAMEPLAYFOOTBALL_BUILD_GAME=OFF \
            -DBUILD_TESTING=OFF \
            -DGAMEPLAYFOOTBALL_BUILD_DOCS=ON \
            -DCMAKE_FIND_PACKAGE_PREFER_CONFIG=OFF

      - name: Generate docs
        run: doxygen Doxyfile

      - name: Upload docs artifact
        uses: actions/upload-artifact@v7
        with:
          name: gameplayfootball-docs
          path: docs/html/
          if-no-files-found: error
