# 28 August 2026

Full working state. Every open item carries its evidence, the files it touches
and the next concrete action, so it can be picked up without re-deriving
anything. Updated as items close.

Prior investigations live in the subagent transcripts `RigdioNative` (rigdio
audio integration, merged) and `ImportVerify` (model import verification);
query them rather than re-running their work.

---

## Closed today

| item | commit | evidence |
|---|---|---|
| Offside cutscene casts the assistant | `9d9ab8` | `e_ChoreoRole_Official` no longer hardcodes `GetReferee()`; two-beat flag-then-protest camera |
| Cinematic vs A replay split | `b95b13e` | `Autorun` marks cinematic; chrome suppressed, prompt offered |
| Replay hang | `48e3441` | `ReplayPage::Process()` returned before advancing playback when `autoRun` was set; 24 of 30 sampled frames were one frozen picture |
| Foul cutscene durations | `8e06e6c` | both call sites read `FoulSequence::CutsceneLength_ms` instead of hardcoding |
| Walkout teardown crash | `8de3441` | `DeleteNode` mid-match tore live geometry; hidden instead |
| Keeper retune | `8de3441` | `gameplay_keeper_sharpness` 0.88 -> 0.66, measured from 3.1 xG against 1.6 actual goals over eight matches |
| Head-presence check | `7c76832` | `body_coverage` judged a body by proximity; a collar ring 0.083 m under the head joint passed as a head |
| Importer budget refusal | `8b94148` | budget is a refusal, not silent amputation; default 250,000 |
| rigdio merge | `e90ad5b` | 1331 tests green, 7 fidelity tests against real exports |
| Joint-binding check | `ce2e44e` | 34 of 107 models have a dead arm joint |
| PES-first philosophy | `15c7995` | see AGENTS.md |
| One-command team import | `ce266d4` | `import_team.py AET/ team.ted` |
| Rebucket test green | `6ae9063` | broke out of `os.walk` after the first directory, so it searched a directory of directories |
| Team art installed automatically | `7139993` | logo and first two kits byte-identical to the hand install; hdg/2hug/dbg all reinstalled |
| Per-team keeper kits | `7139993` | the hand install had the keeper's `g1` sitting in `_kit_03`; `Team::FetchKit` now prefers `<tag>_gk.png` |
| Portraits installed | `1c1d7ea` | both pack naming conventions end in the shirt number; all 23 of 2HUG's convert |
| Team colours imported | `7e3751f` | packs state them in the note file; /hdg/ and /dbg/ were NULL, /lcg/ and /ink/ stored hex that `atof` read as black |
| Handoff covered by the wipe | `16e9a58` | crest covers the cut; uncovers on keepers on their lines, teams in their own halves, two men in the circle |
| Portraits shown and rebound | `66121b9` | all 75 config entries pointed at players they were not written for; four teams 0/23 -> 23/23 |
| Pennant held and simulated | `6aa27af` | raised 992 mm into measured hands; 1,875 points, 80 held, 513 mm sag |
| Entrance camera framed on its own ground | `8090693` | `StadiumToken` matched "stadi", and entrance 020 has no st017 shot; both fixed |
| Imported teams had no formation | `merge wt-presentation` | /hdg/ and /dbg/ were the only rows with formation_xml empty - the two import_team.py installed; panel now reads 4-3-1-2, not 4-1-1-4 |
| Foul camera: PES16 record size | `merge wt-cutscenes` | PES16's camera cut is 276 bytes to PES17-21's 284; the exact-size gate decoded 0 frames, now 300 |
| Offside assistant on the touchline | `merge wt-cutscenes` | officials.cpp spawns linesmen[0] at y -36.5 and calls it North - inverted; picked by live position now |
| Goal celebration honours its cuts | `merge wt-cutscenes` | called `Sample` (row-linear) where the entrance already used `SampleTimeline` |
| Committed suite was red on a clean checkout | `00e6e94` | test_install_team read `ted.STAT_FIELDS`, which only existed in the working tree |
| Offside assistant verified on screen | (capture) | clock 45:34, officials' kit, standing on the touchline with the perimeter behind |
| Headless packs get a body automatically | `77e7c62` | import_team binds 3 of 3 where it bound 1; 2402 renders with head and hands |
| Skydome wedge fixed | `c5ddfb3` | imported domes were lit like walls; AMBIENT/DIFFUSE 1 + SELFILLUM 1 as the engine's own sky does |
| Netting cloth proven to work | `0cc45b8` | `gfviewer --cloth` shoots a ball through it: 12,225 pixels change, woodwork stays black in the difference |

---

## Open

### Import completeness

`import_team.py AET/ team.ted` now does models, squad, stats, name colours,
tactics, sliders, logo, kits, the keeper kit, portraits and team colours.

**Medals need nothing.** They are higher-statted players, and the ted's
per-player stats already carry them - HDG imports a 0.542 to 0.967 base_stat
spread off a 72-97 PES spread, and its three formation presets come through.

Still missing before it is honestly "done":

- **Kits 4 and 5.** Packs ship `p4`/`p5`; the engine's options screen offers
  three. Adding the other two is an engine change, not a reason to drop them.
- **Faces and gloves.** `Faces/` and `Gloves/` are in every pack layout and
  only `Boots/` is imported.

### Rigging

**Arms and hands are broken on every model, in cutscenes and in gameplay.**

Measured with `skin_probe.py` (frame 16 of the lineup anim, hdg_2411), setting
one joint to identity at a time:

| tracks | worst edge stretch | edges past 2x |
|---|---|---|
| all | 14.9x | 791 |
| chest at identity | 14.9x | 791 |
| clavicles at identity | 14.1x | 761 |
| **shoulders at identity** | **2.5x** | **1** |

`left_shoulder` turns **82 degrees**, and linear blend skinning cannot hold a
joint through that: it collapses and drags the torso skin beside it. That is
the ballooning. Naming the joints that own the stretched edges points at the
chest, because chest-bound skin is what gets dragged; the cause is the
shoulder.

Native gameplay anims reach the same angles (090_decel peaks at 84.9 degrees),
which is why it shows in play as well as in cutscenes.

**Next**: this is a skinning problem, not an import one. Dual-quaternion
skinning is the standard answer to exactly this collapse and `skinning.cpp`
already isolates the per-joint transform behind one interface, with its own
test suite. Do not re-open the import: PES's weights arrive intact.

Eight explanations were argued from source and killed - bind-pose mismatch,
quaternion axis conversion, finger joints stealing forearm weights, stale
offsets, animation smoothing, clavicle displacement, wrist tracks, and
proximity-guessed weights. Do not re-derive them.

**Celebrations snap players supine mid-air - mechanism measured.**

Of 387 celebration anims, 46 tilt the body more than 60 degrees and four go past
170 (0057 reaches 179.4). **Not one moves the root vertically**: the worst cases
span -0.09 to +0.12 m. And `player` is the only track carrying translation at
all - every other line is a quaternion.

So a celebration that lays a man on his back rotates the body about a pelvis
still held at standing height (bind z 0.992), which puts his head down and his
feet up with nothing on the ground. That is the "supine mid-air" exactly.

`strip_root` does zero all three components, but only `entrance_pl.py` passes
it, so it is not the culprit for celebrations: the drop is absent from the
source as converted. Either PES carries the pelvis height somewhere
`gani_to_anim` discards (it keeps rotation only for every non-root bone), or the
vertical scale is wrong. Calibrating that is the next step - see
`calibrate_pos_scale.py`, which already measured the horizontal one.

Older note follows.

 `SetChoreoPose` reads
`position.Get2D()`, discarding Z, so a choreography pose cannot lift an actor
off the ground - but the supine snap comes from the celebration clip's own root
motion via `e_FunctionType_Special` selection, not from choreography.
`match.cpp:1989-1994` documents the same failure mode being hit before.

### Cutscenes (wt-cutscenes, merged)

Three root causes, each found in data. `docs/PES_CUTSCENE_ARCHAEOLOGY.md` records
what PES16/17/19/21 each ship. Notably **PES ships no offside camera in any
generation** - 0 canm streams, and PES19's actor record puts both the assistant
and the offender at the origin - so that shot is legitimately ours to author
rather than import.

Verified here on a recorded match: a foul stoppage plays a low close shot on the
official, not the orbit fallback, and hands over to the cinematic replay.

The offside shot is now confirmed too: at clock 45:34 with the OFFSIDE banner
up, the camera frames the assistant in the officials' kit standing on the
touchline, the line at his feet and the perimeter behind him.

Still owed a frame: the goal celebration's hard cuts - backed by the existing
SampleTimeline tests and a drop-in call-site match, but not yet seen. Use
`intro_cutscene_seconds 8` and a six-minute match to get incidents quickly.

### Presentation (wt-presentation, merged)

Delivered and verified on screen: the formation fallback, a readability pass on
the game plan cards, and an editable game plan pitch (grab/move/drop/swap plus a
per-player role submenu, over gui2's directional input - it has no mouse).

Left open by that agent, reported rather than dropped:
- substitutions by drag and drop
- pre-game ball selector, stadium selector with preview, tips loading screen
- the gametips importer from `4cc_25_gametips.cpk`
- a card collision it flagged itself: the top two cards nearly touch after the
  text was enlarged. Either narrow `kCardW`, raise TeamData's
  `minDistanceFraction`, or snap rows the way `FormationGraphicLayout` does.

**PES's own formations are still being thrown away.** The ted carries three
presets and `ted.read_formations` keeps their marks in PES's authoring units,
refusing to guess the scale against the engine's coordinates. Calibrate that and
an imported team gets its real shape instead of a 442.

### Picture

**Vertical banding on the rocks, adboards and clouds.** Reported on screen by
the owner, who traced it to the fringe fix (`2b6fbb0`, "Blur along an edge, not
across it").

Measured over 16 sampled seconds of the same fixture, column-to-column
alternation in the scenery band:

| edge blur | banding |
|---|---|
| off (`graphics_edge_blur_tolerance` 0) | 0.649 |
| selective (0.02, current) | 0.645 |
| flat average (100, the pre-fix behaviour) | 0.561 |

So the banding is **not caused by the blur**: turning it off entirely changes
nothing. It is not mipmapping either - rebuilt with mipmaps off, 0.645 against
0.649. What the old flat average did was smear it over, at the cost of the dark
fringe on every silhouette that `2b6fbb0` removed. Fixing the fringe unmasked
detail that was always there.

The source is the texture. `namek.png` is a 4096 atlas whose islands butt
straight against hard black gutters with no dilation, and the rock faces are
built from many small islands, so every seam is a dark edge; the pink islands
also carry painted strata. Bringing the smear back is not the answer.

Dilating the atlas gutters was tried and **did not help either**: bleeding each
island's colour eight pixels into the transparent gutter (8.4% of namek.png
filled, alpha untouched) measured 0.663 against 0.649 before. Reverted.

So four mechanisms are eliminated by measurement - the edge blur, its depth
test, mipmapping, and the atlas gutter. What is left is the texture's own
painted content: Namek's rock faces carry vertical strata, and the pre-fix flat
average was smearing the whole frame enough to hide them.

That makes it an art/filtering decision rather than a bug. If it should be
softer, the honest levers are a mild filter aimed at the scenery, or accepting
some smear back deliberately - not another hunt for a renderer fault. Do not
re-test the blur, mipmaps or the gutter.

Two candidate fixes were built and reverted for want of evidence: a
slope-aware depth test in `postprocess.frag` (no measurable benefit) and
dual-quaternion skinning (see Rigging).

### Transitions

The pre-match handoff is done (`16e9a58`). The other cutscene exits have not
been checked and use no wipe at all: the goal celebration back to the restart,
the foul cutscene back to the free kick, and the substitution back to play. If
they show the same drift, they want the same treatment - `StartHandoffWipe`
and `RunHandoffWipe` are general, only the caller is pre-match specific.

### Entrance

- **The pennant's own artwork is still wrong**, though it is now held and
  simulated (`6aa27af`). Two importer faults found while wiring it:
  - the flag's texture reads **mirrored** on screen;
  - the competition emblem is composited onto `prop000`, which on this ring is
    four scraps of 11 to 32 vertices, while the visible flag wears `prop001` -
    so the 4cc badge never lands on the thing anybody looks at, and
    `stadium_props.is_pennant_face` documents the opposite of what the data
    does.
- **Torn skydome backdrop - fixed** (`c5ddfb3`). The wedge and the blocky
  facets beside it were one material line: the converter wrote imported domes
  like walls (AMBIENT 0.588, SELFILLUM 0) where the engine's own sky.ase is
  AMBIENT 1, DIFFUSE 1, SELFILLUM 1. Sky mean RGB (54,125,50) with the wedge ->
  (78,181,65) clean.

  Two dead ends, recorded so they are not repeated:
  - **The backdrop's long edges are not shards.** 513 of 9,984 faces carry an
    edge over twenty times the mesh's median, 330 m against 3.89 m. Wiring
    `stretched_cut` into `stadium_to_gf` drops 0.28% of the stadium and *tears
    the landscape apart*: the ground is a few enormous flat polygons among fine
    rock detail, so the legitimate ones are exactly what the heuristic catches.
  - **It was not the constant normal's direction.** Flipping it changes the
    brightness and leaves the wedge identically shaped.

  Still owed: `st017` and `st031` have skies whose `MESH_NORMALS` an older
  `ase_slim` stripped (st002 kept its 1052). Today's slimmer keeps them, so a
  fresh import is right; existing installs want regenerating.
- **Stray PES competition assets.** Left alone deliberately - not committed, so
  they cost a user nothing.
- **Entrance camera collides with models - fixed** (`8090693`). Two faults, one
  symptom. `StadiumToken` read "the first st in the path", which is the one in
  "stadiums", so every ground asked for tracks called `stadi*`, got none, and
  was filmed with whatever shot sorted first. And entrance 020 ships no st017
  shot at all, so it fell back to st000's - a camera at another ground's tunnel
  mouth. A stadium's own shot is now preferred from any family; 001 and 011
  both carry st017.

### Models

**Composite the base body under `hdg_2402` and `hdg_2421`.** Both pack folders
are headless by design - "k2402 - Helldiver Headless" ships 6 meshes with
nothing above y=1.59, and 2421 the same. Head and hands come from PES's base
body, which `import_team.py` must composite in. `hdg_2411` has the same 6 plus
3 head meshes and is correct.

**The pennant staff's shadows stay on the pitch all match.** Reported on screen
at 90:00 with the ball in play: an arc of roughly a dozen dark blobs follows the
centre circle, exactly where the entrance line stood. The models are gone, the
shadows are not - so whatever hides the walkout cast at kick-off is not taking
its shadow casters with it. Look at `HumanoidBase::Hide` and the entrance cast's
parking path (`EntranceCast::ShouldBench` / `SetBenched`) for a shadow node that
outlives the visible one. Owner's ordering: after the composite item above.

### Measure

- **Pass trial above 80 percent - not met.** Three six-minute HDG v DBG trials
  (`passqual.sh mg1/mg4/mg5`, scale 1.6): per-side accuracy 85/71, 76/70, 71/64.
  Pooled mean **72.8%**, best single side 85%. Against the 74/81 measured over
  eight ten-minute matches earlier, this is lower - and the shorter the match
  the better it looks, which points at the later phases rather than at the pass
  model.
- **Goal rate after the keeper retune - answered.** Same three trials: xG
  0.41/0.76/1.38 for 0/1/1 goals, pooled **2 goals from 2.55 xG**. Before the
  retune it was 3.1 xG for 1.6 goals - 52% converted; now conversion tracks xG,
  so `gameplay_keeper_sharpness` 0.66 did what it was meant to and the keeper is
  no longer the binding constraint.

  **Chance creation is.** Those matches make 0.4-1.4 xG in six minutes, which
  scales to 0.7-2.3 over ten against the 3.1 measured before. The shots dried up
  while the finishing was being fixed, and that is the thing to measure next.

### Cutscenes

**Make the match-end cutscene watch the models - fixed** (`d544d88`).

Four faults, all in what the whistle hands over to, all verified on screen:

- **The ground's own crowd was never asked for.** The tag was read as "the first
  st in the path", which finds the one in `stadiums`, so every ground asked for
  `end/audience_stadi`. Same bug as the entrance camera's, in a second copy.
- **The actors were filed by a different rule from their cameras.** The camtrack
  loader keys the flat closing names by family; the choreography loader did not.
  The fallback is silent, so `end/sad` and `end/greet` borrowed the category's
  thirty stagings and drew one at random - a celebration could be staged under a
  dejection camera. After: `end/sad` casts from its own 2, `end/greet` from its 9.
- **A shootout read as a draw**, so a 14-15 shootout ended with nobody
  celebrating. It now asks the shootout when the run of play is level.
- **The match HUD was drawn over all of it.** Only the entrance ever took it down.

Went from "2 shots, 13 s, drawn" to "4 shots, 26 s" playing audience_st017 into
sad into greet into photo, with a dozen players staged along the touchline
walking to the stand. Cast selection is logged now, beside the camera.

One fixture note, not an engine fault: `menu_smoke_gameover_hold_ms` defaults to
1000, so the smoke harness quits one second after the whistle and the ceremony
cannot be observed at all. Set it to 32000 to watch one.

