# 03 September 2026 - goal sequence, penalty netting, the animation eye pass

## Fixed: goal -> celebration -> replay stopped firing, celebration repeated

Cause, one for both symptoms: the goal cast took its length from the .chor's
`cycleFrames` (`GetLastFrame`), which is PES's whole camera beat, not the
performance. Measured on `goal_celebrate_0057_base`: performance 10.19 s,
beat 26.95 s, capped by `kLongestCelebration_ms` to 20 s. So
  * the clips repeated to fill the 20 s window (`SetChoreoPose` wraps the
    frame, which is right for a walk-on marking time and wrong for a
    performance), and
  * the referee, which schedules the restart the tick after the goal off the
    *default* 9 s, called `ResetSituation` at 10.5 s and cleared the goal state
    the replay trigger waits on. No replay, ever.

Fix (`match.cpp`, `goalsequence.*`): the cast's length is the last actor's clip
ending; an actor who is done holds his last pose; `RestartPrepareAt_ms` takes
the celebration length and Match pushes the referee's buffer back once the clip
is chosen (`AlterSetPiecePrepareTime`). `ScheduleIsConsistent` and a new test
assert the order for every length up to the cap.
Verified in `/tmp/seqcheck.mp4` + `.log`: both goals log
"performance runs 10190 ms of a 26950 ms beat" and "goal replay: celebration
ran 10190 ms, replay reaches 20190 ms back"; on screen goal -> celebration ->
wipe -> replay -> wipe -> kickoff.

## Fixed: a scored penalty flew through the net

`Match::SetGoalScored(false)` also cleared `ballIsInGoal`, and the netting in
`Ball::Process` is gated on that flag. The shootout calls it the moment it sees
the ball cross, i.e. 1-4 ticks after the ball entered - so the netting switched
off with the ball still doing 25 m/s inside the goal, and it left the stadium.
Netting is now only cleared by a reset. Measured over a shootout
(`/tmp/penprobe.log`, ball position logged every tick): 7 of 7 scored penalties
now stop at 57.67-57.95 m against a rear panel at 57.66 m, one of them struck
at 38 m/s. `ball_physics_test.cpp` gained the netting's first two tests.

Open, same report: a penalty *aimed wide* crosses the byline outside the posts
(measured y=6.1 m) and there is nothing behind the goal - no net, no stand - so
it rolls 28 m out (x=-83.6) with the camera following it off the pitch. Tried
killing the ball's momentum in `FinishKick`: wrong, because the verdict lands
the moment the ball crosses, so goals froze at the line (max |x| 55.78 instead
of 57.8). Reverted. The real fix is perimeter geometry behind the goal.

## The animation eye pass: my 02-09 diagnosis was wrong

**Imported match animations are never selected.** Measured with a temporary
probe on every anim switch, two full matches: 4,522 selections traced, 915 PES
clips loaded, **0 selected**. And 0 with the *pre-fix* bucketing restored (111
clips copied back into walk/dribble), so the mis-bucketing was not what put a
striding clip under a moving player either. Also measured: no clip with < 0.25 m
travel is ever given to a player above 2 m/s, no travelling clip is ever
stalled (1 near-miss in 4,522, a legitimate decel), and no player plays four
travelling clips inside 1.5 s while moving < 0.4 m.

So `270990a` fixed a real inconsistency in the installed data and did not fix
the owner's defect. What he saw is still unexplained. Leads, in order:
  1. `joint_binding.py --all`: **25 of 121 models have a dead joint**, 6 of them
     in the legs (lcg_2701 both knees and ankles, smg_2579 and smg_2587 both
     knees, smg_2590 right knee). A leg that cannot bend glides.
  2. The models he named are coarse: FILTHY MONKEYS is smg_2583, a 573-vertex
     N64 Fox whose shins are rigid blocks with a gap at the knee (rendered:
     /tmp/smg2583_15.png). At match distance a stride on that geometry reads as
     a shuffle. That is the pack's own art, not ours to change.
  3. Not yet excluded: the autogenerated transitions, which are 2,166 of the
     4,522 selections and 1,427 of the fast ones.
Next: repair the binding in `fmdl_to_fullbody.py` (every joint with geometry
near it must own at least one vertex), which is verifiable with
`joint_binding.py`, then re-run the eye pass against a fresh capture.

## Fixed: 4cc boots-slot characters had dead knees

Found while hunting the "slipping" report, and it is the best candidate for it.
4cc packs a whole character into the **boots** slot, so the export's bone
mapping is whatever that slot allows. Read straight from the pack:
`k2587 - Subcon deez Warts` names exactly two leg bones, `sk_foot_l` and
`sk_foot_r`, and no thigh or knee at all. The installed model therefore had 785
vertices a side on the ankle, 0 on either knee - a leg that cannot bend, so it
swings rigidly from hip to foot. `joint_binding.py --all`: 25 of 121 models had
a dead joint, 6 of them in the legs.

`fmdl_to_fullbody.rebind_stray`: a vertex whose mapped joints are all further
than 0.15 m away, when a rig joint sits closer, is rebound by position through
the `nearest_joints` path the importer already uses for unmapped props. A
healthy vertex sits on the joint it is mapped to and is untouched; a cape tip
far from everything is untouched too (tested).

Re-imported from the pack: smg_2587 left_knee 0 -> 290, right_knee 0 -> 330,
smg_2579 18 -> 89 and 36 -> 103, and `joint_binding.py` reports 0 dead joints
for both. smg_2587 re-rendered whole (/tmp/w2587_01.png). Still to do: rerun
the import for every team whose pack is on disk (smbg, dbg, lcg, ink, 2hug,
hdg) so the other 23 models get the same repair, then look at a capture.

---

# Open items, written down (end of 03-09)

Six things are open, in the owner's stated priority. Everything below has its
evidence with it so none of it has to be re-derived.

## 1. Performance: the game runs at 31 fps and has to run at 60  [PARAMOUNT]

Measured, Release build (`build/` is `-O3 -DNDEBUG`), headless on the card
(`SDL_VIDEODRIVER=offscreen`), dbg v smbg at st017, with a temporary probe in
`SwapBuffers` and `perf record -F 199 -g`:

    FPSLOG frames=161 fps=32.1 mean=31.1ms p50=30.9 p95=34.3 max=43.8
    FPSLOG frames=163 fps=32.4 mean=30.8ms p50=30.8 p95=34.3 max=36.3

**31-32 fps, 31 ms a frame, p95 34 ms.** Steady, not spiky: this is per-frame
work, not a stall. 60 fps needs 16.7 ms, so the frame has to halve.

Every capture in this session was made on that build, so what the owner has
been watching is a 31 fps game paced into a 60 fps container by the recorder
(the pacer duplicates frames, which is why the videos look smooth and are not).

Profile, self time (`perf report --no-children`):

|share|symbol|
|---|---|
|~43%|one libc routine at `libc+0x1765xx` (memmove/memcpy family), called from `blunted::Geometry::OnUpdateGeometryData(bool)` - 8.7 + 8.2 + 8.1 + 7.6 + 3.0 + 2.8 + 2.3 + 2.3 across its instruction cluster|
|14.8%|`HumanoidBase::UpdateFullbodyModel(bool)`|
|5.3%|`Skinning::TransformPoint`|
|5.2%|`blunted::Vector3::FastNormalize`|
|3.3%|`Skinning::AddWeighted`|
|2.3%|`Skinning::TransformDirection`|

So roughly **43% copying vertex buffers and 27% skinning them**, every frame,
for 22 bodies - and the bodies are now the imported ones (`--max-tris` admits
up to 250,000 triangles a character; the largest DBG character is 212,417).
The engine CPU-skins every unique vertex of every player each body tick and
has no LOD (AGENTS.md).

Next, in order of expected win per unit of work:
 1. `Geometry::OnUpdateGeometryData` - find out how many times a frame's
    vertex data is copied and stop the redundant ones (skin straight into the
    buffer that is uploaded rather than into a temporary).
 2. LOD for distant players: a decimated mesh, or reuse `BodyNeedsSkinning`'s
    distance/phase machinery to skip more aggressively.
 3. Normals: `FastNormalize` per vertex on the CPU is work the vertex shader
    would do for nothing.
 4. GPU skinning is the real answer and the biggest change; only after the
    three above have been measured.
Probe is uncommitted (`GF_FPSLOG` in `opengl_renderer3d.cpp`) - strip it
before committing anything, and re-measure the same fixture after each step.

## 2. Presentation: the replay wipe is missing, and the cutscene order is wrong

Owner's instruction, which fixes two things at once: **play the stoppage
cutscene first, then reset the players into their restart positions while the
replay wipe covers the screen.** Today the reset happens in the open, which is
what makes the offside restart look broken (item 3) and what leaves the wipe
unused. `Match::RunHandoffWipe` / `handoffWipe*` and
`SetPieceLaws`/`Referee::Process` are the pieces; `replay_wipe_dir` supplies
the frames (`import_wipe.py`).

## 3. Offside restart: the opponent does not retreat

Reported in `showdown2.mp4` at video 04:42, ingame clock 33:03: an offside is
given, THE CHAMP! is supposed to clear away from the restart and does not, he
finishes with the ball at his feet, and the keeper takes the kick into him.
`SetPieceLaws` already has the law (`kRetreatRadius` 9.15 m,
`ClearsThePenaltyArea`, `IntrudesOnBallRadius`, `MayRestart` with a 6 s cap) so
the question is which of those the offside path does not go through - the
6-second cap expiring would produce exactly this. Not yet diagnosed.

## 4. SHIDDY (smg_2580) is a re-import regression

The re-imported model has legs, which it should not - the character is a Boo -
and the owner reports the boo mesh itself is mangled. Before the re-import it
was correct. It was NOT composited (the log only shows 2582 taking a stock
body), it has one mesh group and 6,949 vertices, and `body_coverage` calls it
whole. So the change is in this model's own geometry or its texture's alpha,
not in a composite. Candidates: the kit texture that `install_kit_texture`
picked (`p1` glob) losing the invisible-kit alpha, `--max-edge 0.15` cutting a
soft round mesh, or `strip_stretched_tris`. Compare against a pre-re-import
frame (`/tmp/slip/*.png`, `pen_*.png` from 02-09 carry SHIDDY in shot).

## 5. Verify the animation work on a release capture with an entrance

Owner wants a short capture on the release build with the entrance cutscene
playing, to judge whether locomotion and the arms are still right after the
position-scale fixes (`d598c65`, `725ef24`). Not yet run.

## 6. Windows in GETTING_STARTED.md

Researched, not written: `.\build.ps1` (bootstraps vcpkg from `vcpkg.json`),
`.\run.ps1`, `scripts\package_windows.ps1`; CMake copies `media/` beside the
binary on Windows instead of symlinking (`cmake/link_media.cmake`,
`FALLBACK_COPY` is `WIN32`), so every build configuration holds its own copy of
whatever the import produced; the Python importers are portable (no POSIX-only
modules) but need ffmpeg on PATH for chants/wipes and an explicit `--ganis`
path instead of the `/tmp/ganis` default; `extract_stadium_packs.sh` and
`convert_stadiums.sh` need Git Bash or WSL; `tools/showcase.sh` cannot run
there at all (mkfifo, xvfb, timeout), so recording on Windows means
`frame_recording_path` to a file and encoding afterwards. None of it verified
on a Windows machine - say so in the doc.

## Still open from earlier in the session

* A penalty aimed wide leaves the stadium: nothing behind the goal to stop it.
  Perimeter geometry is the fix; killing the ball at the verdict is not (it
  froze goals at the line, reverted).
* Imported match clips are candidates but never selected: 92% of the candidate
  set for trap/interfere/deflect is imported (`SELLOG` measurement) and
  `Humanoid::GetBestCheatableAnimID` takes the first that can reach the ball,
  which is always a stock clip. Unexplained; the position-scale fix did not
  change it.
* 25 models still have a dead joint, 10 of them in dbg and ink whose packs are
  not on disk. `joint_binding.py --all` is the check.
* The corner-kick camera at st017 sits behind a Namek dome prop for ~3 s.

## Performance pass 1: 36 -> 76 fps on this machine (2.1x headroom)

Measured on the same fixture throughout - dbg v smbg at st017, Release
(`-O3 -DNDEBUG`), headless on the card, medians of 5-second windows over
several 95-second runs with nothing else on the machine. Early numbers in this
file that disagree were taken with `perf record` attached or with a second
engine running; those are void.

|state|frame|fps|
|---|---|---|
|before|27.8 ms|36.0|
|+ vertex copy: by-reference, changed-attributes-only|22.4 ms|44.6|
|+ distant bodies at half rate (now the default)|18.1 ms|55.2|
|+ partial vertex-buffer upload (dynamic prefix only)|13.8 ms|72.5|
|+ flat per-vertex weight table|13.2 ms|75.9|

What each one was:
* `Geometry::OnUpdateGeometry` copied `GeometryData::triangleMeshes` **by
  value**, deep-copying every submesh's index vector every frame (~14 MB and 84
  allocations a frame over 28 bodies), then memcpy'd all five attribute arrays
  into the vertex buffer although skinning only rewrites positions and normals.
  Both fixed: a reference, and a per-GeometryData mask of which elements are
  dynamic (`SetDynamicElements`, set by `PrepareFullbodyModel` - tangents only
  when a normal map is bound, texture vertices never). 2.7 GB/s -> 1.1 GB/s.
* The GL thread then orphaned the whole buffer and memcpy'd all of it again.
  `UpdateVertexBuffer` now takes a `dynamicFloats` prefix: the layout is
  element-major, so positions+normals are the first two fifths, and both
  ping-pong slots already hold the static tail from `CreateVertexBuffer`.
* `animation_halve_distant_bodies` defaults to true (see the code comment).
* `WeightedVertex` held a `std::vector<WeightedBone>` per vertex - a heap chase
  per vertex over ~1M vertices a frame. Flat `jointID[4]/weight[4]` with a
  count; `vertexID` deleted, it was always the loop index.
* The four `Skinning` kernels and `Vector3::FastNormalize`/`GetDotProduct` are
  `inline` in their headers: the build has no LTO, so out of line they were
  five to eight real calls per vertex (PlayerRuntime's objdump).
* `Animation::GetInterpolatedValues` walked the key map from `begin()` and
  pushed every passed key into a fresh vector; now `lower_bound` + `std::prev`.
  1,599 of 1,892 installed clips are keyed on every frame, and this runs twice
  per node per put and once per frame of every clip at load.

Verified: 1,381 tests pass, and a 1-minute-halves capture renders players with
correct textures and no torn geometry (/tmp/pc_zoom.png), which is what the
partial upload had to be checked against.

### The remaining ladder, for the next pass
`UpdateFullbodyModel` is now 45.7% of self time on its own and the memmove is
down to ~19%. In expected order of value for a weak machine:
 1. Skin straight into the vertex buffer. The middle copy (mesh array ->
    concatenated buffer) only exists to concatenate; ~19% of the frame.
 2. Mesh LOD. The imported bodies are 20k-100k+ vertices with no LOD at all;
    a decimated mesh for anything past ~20 m is the biggest single lever left
    and it is importer work, not engine work.
 3. Off-camera culling: bodies behind the camera are skinned in full. Needs the
    camera basis on the game thread; getting the forward sign wrong would skip
    visible bodies, so it wants a frame to check against.
 4. Skip normal skinning for distant bodies (shading only), ~40% of their
    per-vertex cost.
 5. SIMD: the loop is scalar. `-march=x86-64-v2` or a dispatched AVX2 path for
    the skinning translation unit only - but v3/AVX2 is a 2013 floor, which is
    a distribution decision rather than a free win.
