# 04 September 2026 - cutscene audit, every category

Scratch for this pass: `/home/z/.claude/jobs/f858a344/tmp/CutsceneAudit/` (captures in
`caps/`, frames in `m1f/`, `m2f/`, the jump analyser `jumps.py`, the capture harness
`cap.sh`). Baseline entrance capture is the 03-09 `ents/009.mp4`.

## Inventory (what is installed, how the pool is chosen)

|category|camera tracks|choreographies|pool chosen by|
|---|---|---|---|
|entrance (`ent/<family>/`)|per family and ground|per family and ground|`LoadPrematchShots`/`LoadPrematchStagingIndex`: this ground's variant, else ent_009|
|foul|2 (`foul_injury_card_y01`, `_r01`, both the same static shot 5.4 m out, 3.5 m up, 6 s)|30 top level + card_red 10, card_yellow 20, warning 8, injury 7, no_card 9, protest 11, referee_run 12|`StartCutscene("foul/<sub>")`, subpool by directory, falls back to the whole `foul` pool; `(actualTime/10 + score) % size`|
|offside|0|7|choreography only; broadcast camera holds on the assistant, then the offender|
|change|78 (66 of them `change_stand_stXXX`, one per PES ground)|30|`StartCutscene("change")` on 35% of substitutions|
|goal|731|534|by celebration name (`celebrations.txt`), `StartGoalCast`|
|timeup|9 (1 half-time for st000, 1 full-time, 7 post-match pick-ups)|30 (9 half-time walk-offs, one per PES ground; 21 full-time reactions)|`StartCutscene("timeup", 6 s)` at the start of the second half and of extra time|
|pk|21|0|`StartCutscene("pk", 7 s)` when the shootout starts|
|end|140|30|`CutsceneSequence::ClosingStages`: audience_<ground>, joy/sad, greet, photo|
|result|65 (60 per-ground `result_001_stXXX`, 2 `result_002` exterior aerials, 3 `result_003`)|0|`result/<result_cutscene_id>` else whole pool|
|injury|0 (PES ships none; `foul/injury` has 7 stagings)|7|never asked for before today|

A category with no camera and a choreography plays the choreography under the broadcast
camera (offside). A subpool with no camera falls back to the whole category
(`foul/warning` -> `foul`, i.e. the card shot).

## Findings and fixes (details per section below)

(filled in as the pass goes)
